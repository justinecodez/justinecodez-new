services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: justine-portfolio
    restart: unless-stopped
    networks:
      - gateway

networks:
  gateway:
    external: true
